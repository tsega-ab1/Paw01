# 🐾 PAW Backend
PAW Life OS — 8 AI agents, Supabase PostgreSQL, 6 REST endpoints. Deploys to Render.

## Setup
1. Copy `.env.example` to `.env` and fill in your keys
2. Run `npm install`
3. Run `node scripts/setup-db.js` to initialize the database
4. Run `npm start`
